fn main() {
    let empty_string= String::new();
    println!("The length of the empty_string is {}", empty_string.len());

    let content_string = String::from(" ComputerScience");
    println!("The length of content_string is {}",content_string.len() );
}
